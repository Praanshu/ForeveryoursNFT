After installing the repo in any folder

1. Install npm using 'sudo npm install'
2. Run the the app by entering folder using 'cd /foreveryoursnft'
3. then, 'cd /myapp'
4. Then use 'sudo npm start' to run locally.
